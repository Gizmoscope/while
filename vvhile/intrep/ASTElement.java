package vvhile.intrep;

/**
 * When an abstract syntax tree (AST) is build, AST-elements are added to a stack
 * an then processed by the AST-compiler. In other words, the AST-compiler can
 * handle all data specified as AST-element.
 * 
 * @author markus
 */
public interface ASTElement {
    
}
